# AAU BAM-1022 PM2.5 Data Preparation for OpenAQ

This repository contains the **data processing workflow** used to prepare the Addis Ababa University (AAU) GEOHealth BAM-1022 PM2.5 dataset for **OpenAQ submission**.  

The workflow ensures that each measurement is non-imputed, follows OpenAQ framework requirements, and is reproducible for future submissions.


## 📍 Station Information

- **Name:** AddisTASHGEOHealth  
- **Location:** Tikur Anbessa Specialized Hospital (TASH), Addis Ababa  
- **Coordinates:** 9.01523, 38.74291  
- **Instrument:** BAM 1022  
- **Parameter:** PM2.5  
- **Duration:** 3600 seconds (hourly measurements)  

This metadata applies to all measurements in the dataset and is included in every row during data preparation.

---

## 🔐 Data Policy

- Raw measurement data is **confidential** and **not included** in this repository.  
- Only processing scripts, sample outputs, and documentation are available publicly.  

This repository contains:  
- **Full R formatting script** – Converts the entire AAU BAM dataset (April 1, 2017 – September 30, 2023) into OpenAQ-ready format  
- **Sample output CSV** – Demonstrates the expected OpenAQ-compliant structure  
- **Documentation** – Describes the field structure, validation rules, and framework compliance  

---

## 📂  Folder Structure

The repository follows a standardised layout. Create the following folders in the repository:

```
AAU-BAM-OpenAQ-Data-Preparation/  
│  
├── `input/` # Confidential raw BAM CSVs (not committed)    
├── `output/` # OpenAQ-ready CSVs generated by the script  
├── `aau_bam_openaq_formatting.R` # R scripts for data processing  
├── README.md # This file  
└── .gitignore # To protect raw data and temporary files  
```

> The folder structure separates **raw data**, **processing scripts**, and **sample outputs** to maintain confidentiality and reproducibility.

---

## 📦 Output Format (OpenAQ Framework)

Each row represents **one non-imputed measurement** and includes the following columns:

| Column        | Description |
|---------------|-------------|
| `name`        | Station name |
| `location_code` | Unique station identifier |
| `latitude`    | WGS84 latitude of the station |
| `longitude`   | WGS84 longitude of the station |
| `duration`    | Duration of the measurement in seconds (e.g., 3600 for hourly values) |
| `parameter`   | Name of the pollutant (e.g., pm25) |
| `unit`        | Unit of the measurement (e.g., ug/m3) |
| `sensor_code` | Instrument used (e.g., BAM 1022) |
| `value`       | Measured value |
| `datetime`    | ISO-8601 timestamp with timezone (e.g., `2023-12-01T22:30:38+03:00`) |

> **Uniqueness Rule:**  
> The combination of `datetime + parameter + value` must be unique for all rows.

---

## 📊 Sample Output

Here is an example of how the processed data looks:

| name                 | location_code                                               | latitude | longitude | duration | parameter | unit  | sensor_code | value | datetime               |
|----------------------|------------------------------------------------------------|----------|-----------|---------|-----------|-------|-------------|-------|------------------------|
| AddisTASHGEOHealth   | Addis Ababa, Tikur Anbessa Specialized Hospital (TASH), GEOHealth | 9.01523  | 38.74291 | 3600    | pm25      | ug/m3 | BAM 1022   | 18.4  | 2023-01-01T00:00:00+03:00 |
| AddisTASHGEOHealth   | Addis Ababa, Tikur Anbessa Specialized Hospital (TASH), GEOHealth | 9.01523  | 38.74291 | 3600    | pm25      | ug/m3 | BAM 1022   | 21.7  | 2023-01-01T01:00:00+03:00 |
| AddisTASHGEOHealth   | Addis Ababa, Tikur Anbessa Specialized Hospital (TASH), GEOHealth | 9.01523  | 38.74291 | 3600    | pm25      | ug/m3 | BAM 1022   | 19.3  | 2023-01-01T02:00:00+03:00 |

> This sample demonstrates the **framework-compliant output structure** without exposing real measurements.

---

## ⚙️ How to Use

1. Place your confidential raw BAM CSV file(s) in the `input/` folder.  
2. Run the R script(s) in `aau_bam_openaq_formatting.R` to generate OpenAQ-ready CSVs in `output/`.  
3. Use `sample_output` for testing, reference, or documentation.  

---

---

## ⚙️ Workflow & Steps

1. Read the raw AAU BAM CSV dataset (hourly PM2.5 measurements).  
2. Rename columns and parse datetime.  
3. Assign correct timezone (Africa/Addis_Ababa) without shifting the original time.  
4. Format datetime into ISO-8601 format (`YYYY-MM-DDTHH:MM:SS±HH:MM`).  
5. Add OpenAQ-required metadata: name, location_code, latitude, longitude, duration, parameter, unit, sensor_code.  
6. Keep only non-imputed measurements (rows with NA in `value` are removed).  
7. Ensure uniqueness (`datetime + parameter + value`).  
8. Export CSV to `output/` folder for submission.  
9. Sample output is included for reference and transparency.

---

## 📄 Documentation

Detailed explanation of the **data preparation framework** and **processing steps** is available in step-by-step of `aau_bam_openaq_formatting.R`.  

Includes:  
- Non-imputed value handling  
- ISO-8601 datetime formatting with timezone  
- Metadata structure  
- Duplicate/uniqueness validation  
- Notes on reproducibility and framework compliance

This ensures **reproducibility**, **transparency**, and compliance with OpenAQ non-imputed measurement requirements.

---

## ✅ Notes

- The workflow covers the **full AAU BAM dataset (April 1, 2017 – September 30, 2023)**.  
- Only synthetic/sample outputs are included for documentation and GitHub sharing.  
- After validation, the full formatted dataset is ready to be shared with OpenAQ contacts.  

## 🤝 Contributing

*Pull requests are welcome!! Please fork this repo and create a feature branch.*

# Happy coding!! 🚀